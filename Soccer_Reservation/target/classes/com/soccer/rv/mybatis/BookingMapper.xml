<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="booking">

	<insert id="insert" parameterType="bookingDto">
		INSERT INTO reservation_form (rUser,rAddr,rTime,phone01, phone02, addr1, addr2, addr3, userNumber, orgName, etc)
		VALUES(#{rUser}, #{rAddr}, #{rTime}, #{phone01}, #{phone02},
		#{addr1}, #{addr2}, #{addr3}, #{userNumber}, #{orgName}, #{etc})
	</insert>
	
	<select id="getData" parameterType="String" resultType="bookingDto">
		select * from users where id=#{id}
	</select>
</mapper>